== My Enumerable

Implementation of several of the enumerable methods utilizing a custom module, a custom class acting as an array wrapper, and an rspec test suite to confirm that the custom methods are functioning in the same way as the methods defined in the Enumerable module.
